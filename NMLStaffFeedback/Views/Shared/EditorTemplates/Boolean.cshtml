@model bool?

@if (ViewData.ModelMetadata.IsNullableValueType)
{
    @(
        Html.Kendo().DropDownListFor(model => model)
            .Items(items =>
            {
                items.Add().Text("Not answered");
                items.Add().Text("Yes").Value("true");
                items.Add().Text("No").Value("false");
            })
            //specify that the default value binidng should not be set and set the nullableBoolean binding
            .HtmlAttributes(new { data_skip = true, data_bind = "nullableBoolean:" + ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty) })
    )
}
else
{
    <input type="checkbox" @(Model.Value ? "checked='checked'" : "") />
}